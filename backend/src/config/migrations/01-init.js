/**
 * Initial Schema Migration
 * Note: Database schema is automatically synced via Sequelize (backend/src/config/db.js).
 * This folder is reserved for future manual migration scripts using Sequelize CLI or Umzug.
 */
module.exports = {
  up: async (queryInterface, Sequelize) => {
    // Write migration code here.
  },
  down: async (queryInterface, Sequelize) => {
    // Write rollback code here.
  }
};
